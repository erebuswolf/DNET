using UnityEngine;
using System.Collections;

public class TestID : MonoBehaviour {
    public long ID;

    // Use this for initialization
    void Start () {
        Debug.LogWarning("ID is " + ID);
    }
	
    // Update is called once per frame
    void Update () {
	    
    }
}
